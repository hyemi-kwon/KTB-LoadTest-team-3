# 부하 테스트 프로젝트 개인 회고

## 1. 담당 업무

이번 프로젝트에서 다음 작업을 담당했다.

- S3·CloudFront 기반 파일 오프로딩 구조 구축
- 백엔드 S3Storage 연동
- GitHub Actions 기반 백엔드 CI 구축
- MongoDB 샤딩 클러스터 설계 및 구축
- MongoDB 샤드 키 선정 및 백엔드 연결 전환
- Redis Replica 및 Redis Cluster 구성
- MongoDB와 Redis의 데이터 분산 방식 분석
- 부하 테스트 결과 수집 및 병목 분석

전체 인프라 중 파일 오프로딩과 백엔드 CI를 담당했으며, MongoDB와 Redis의 쓰기 분산 구조를 설계하고 검증하는 데 집중했다. ALB 라우팅, 프론트엔드 수평 확장, JVM·Tomcat 튜닝은 팀원들과 역할을 나누어 진행했다.

---

## 2. S3·CloudFront 파일 오프로딩

이미지와 PDF 같은 파일 요청이 백엔드에 집중되지 않도록 S3와 CloudFront를 이용한 파일 처리 구조를 구성했다.

```text
업로드:   사용자 → ALB → Backend → S3
다운로드: 사용자 → CloudFront → S3
```

백엔드는 파일 업로드와 메타데이터 처리를 담당하고, 실제 파일 저장은 S3가 담당하도록 구성했다. 파일 다운로드는 CloudFront가 처리해 백엔드가 API와 WebSocket 요청에 집중할 수 있도록 역할을 분리했다.

S3의 퍼블릭 액세스는 모두 차단하고 CloudFront의 OAC를 통해서만 객체에 접근할 수 있도록 설정했다.

### 검증하지 못한 부분

S3·CloudFront 인프라와 백엔드 연동은 기존에 완료되어 있었지만, 반복적인 배포 과정에서 S3 관련 환경변수가 실제 부하 테스트 서버에 반영되지 않았다.

당시 프론트엔드 부하에 대응하기 위해 여러 변경 사항을 짧은 시간 안에 연속으로 배포하고 있었다. 각 배포 직후 파일 업로드와 조회 경로를 확인하지 않은 채 다음 변경을 진행하면서, 이미지가 S3에 저장되지 않고 있다는 사실을 즉시 발견하지 못했다. 부하 테스트 해커톤 실전 직전에 이미지 처리 경로를 확인하는 과정에서 문제를 발견했고, 원인이 S3 환경변수 누락이라는 점을 뒤늦게 파악했다.

여러 배포가 이미 누적된 상태였기 때문에 어느 변경에서 문제가 발생했는지 추적하는 데 시간이 걸렸다. 남은 시간 안에 두 백엔드 서버의 환경변수와 파일 처리 경로를 모두 수정하고 다시 검증하기 어려워, 결국 파일 오프로딩이 적용되지 않은 상태로 테스트를 진행했다.

이번 경험을 통해 배포 완료는 프로세스가 실행된 시점이 아니라 핵심 기능의 정상 동작까지 확인한 시점이라는 점을 배웠다. 앞으로는 변경 사항을 한 번에 누적해 배포하지 않고, 배포 단위를 작게 유지한 뒤 각 단계마다 필수 기능을 확인할 것이다. 특히 S3 업로드와 CloudFront 조회를 배포 후 스모크 테스트에 포함하고, 테스트를 통과해야 다음 배포로 넘어가도록 절차를 구성하고 싶다.

개발 및 사전 검증 단계에서는 파일이 S3에 정상적으로 저장되고, 다운로드 요청이 CloudFront를 경유하며 캐시가 적용되는 것까지 확인했다. 다만 부하 테스트 해커톤 실전 환경에서는 S3 설정이 누락되어 적용 전후의 성능을 비교하지 못했다. 따라서 파일 오프로딩의 기능적 동작과 별개로, 백엔드 부하 및 응답 시간 개선 효과는 이번 결과에서 검증된 성과로 주장하지 않았다.

---

## 3. GitHub Actions 기반 CI 구축

Pull Request가 생성되면 Java 25 환경에서 Maven 테스트를 실행하고, 테스트가 성공한 경우에만 `linux/amd64`용 Docker 이미지 빌드를 검증하도록 백엔드 CI를 구성했다. 이를 통해 컴파일 및 테스트 실패가 포함된 이미지가 배포 단계로 넘어가지 않도록 했다.

`main` 브랜치에 코드가 반영되면 테스트와 이미지 빌드를 거쳐 Docker Hub에 커밋 SHA 기반 태그로 이미지를 푸시하도록 구성했다.

```text
<DOCKERHUB_USERNAME>/ktb-bootcampchat:backend-{commit_SHA}
```

항상 변경될 수 있는 `latest` 태그는 사용하지 않았다. 대신 커밋 SHA 태그를 사용해 어떤 코드로 생성된 이미지인지 명확히 추적하고, 문제가 발생했을 때 특정 버전의 이미지를 선택해 이전 상태로 돌아갈 수 있는 기반을 마련했다.

다만 이번 프로젝트에서는 CI까지만 자동화하고 실제 배포는 수동으로 진행했다. 이 때문에 CI에서 성공한 코드와 이미지가 실제 서버 설정까지 정상적으로 반영됐다고 보장할 수 없었다.

---

## 4. MongoDB 샤딩

### 샤딩을 선택한 이유

채팅 서비스는 다수의 사용자가 동시에 메시지를 저장하는 쓰기 요청이 많이 발생한다. 이번 테스트 역시 여러 사용자가 채팅방을 생성하고 메시지를 전송하는 시나리오였다.

처음에는 MongoDB Replica Set을 이용한 분산을 검토했다. Replica Set은 Primary 데이터를 Secondary에 복제하므로 장애 대응과 데이터 복구에 적합하지만, 일반적인 구성에서는 쓰기 요청이 Primary에 집중된다. 따라서 쓰기 부하 자체를 여러 노드로 분산하기는 어렵다고 판단했다.

반면 샤딩은 데이터를 여러 샤드에 나누어 저장하고 서로 다른 샤드에서 쓰기를 처리할 수 있다. 이에 따라 메시지 쓰기 분산을 목적으로 MongoDB Sharding을 적용했다.

### 클러스터 구성

기존에는 모든 백엔드가 하나의 MongoDB에 직접 연결하고 있었다.

```text
Backend 1~4
    │
    ▼
단일 MongoDB
```

이를 다음과 같은 샤딩 구조로 변경했다.

```text
Backend 1~4
    │
    ▼
  mongos
    │
    ├── shard1RS
    └── shard2RS

configRS
└── 클러스터 메타데이터 관리
```

각 구성 요소의 역할은 다음과 같다.

- `shard1RS`: 기존 MongoDB EC2를 Replica Set으로 전환
- `shard2RS`: 신규 데이터 샤드
- `configRS`: 샤딩 클러스터 메타데이터 관리
- `mongos`: 백엔드 요청을 적절한 샤드로 전달

테스트 환경의 자원이 제한되어 각 Replica Set은 단일 멤버로 구성했다. 따라서 운영 수준의 고가용성을 구현한 것은 아니며, 샤딩 정책과 쓰기 분산을 검증하는 데 목적을 두었다.

### 샤드 키 선정

쓰기량이 가장 많은 `messages` 컬렉션에 채팅방 ID인 `room`을 해시 샤드 키로 적용했다.

```javascript
sh.shardCollection(
  "bootcamp-chat.messages",
  { room: "hashed" }
)
```

현재 부하 테스트는 여러 사용자가 서로 다른 채팅방에서 메시지를 전송하는 시나리오였다. 따라서 `room` 값을 해싱하면 채팅방별 쓰기 요청을 두 샤드에 비교적 고르게 분산할 수 있다고 판단했다.

백엔드의 MongoDB 연결 주소도 개별 MongoDB 인스턴스가 아닌 `mongos`를 바라보도록 변경했다.

```env
MONGO_URI=mongodb://<MONGOS_PRIVATE_IP>:27017/bootcamp-chat?maxPoolSize=25&minPoolSize=2&waitQueueTimeoutMS=5000
```

이를 통해 백엔드는 데이터가 어느 샤드에 저장되는지 알 필요 없이 기존과 같은 방식으로 MongoDB를 사용할 수 있었다.

### 샤딩 결과

468 VU의 분산 브라우저 테스트에서 약 11만 5천 건의 HTTP 요청을 처리했다. MongoDB에는 13,000건 이상의 메시지가 저장됐으며, 두 샤드에 다음과 같이 분산됐다.

| 샤드 | 메시지 수 | 비율 |
| --- | ---: | ---: |
| shard1 | 7,634건 | 54.27% |
| shard2 | 6,431건 | 45.72% |

이를 통해 `{ room: "hashed" }` 샤드 키가 실제 부하 상황에서 메시지 데이터를 두 샤드에 분산한다는 점을 확인했다.

다만 샤딩 적용 전과 동일한 조건의 테스트 결과를 남기지 못했기 때문에 성능이 몇 퍼센트 개선됐다고 판단할 수는 없다. 이번 테스트에서 검증한 것은 성능 향상률이 아니라 **13,000건 이상의 메시지가 약 54:46으로 분산됐다는 사실**이다.

### 단일 채팅방에 대한 한계

`room: hashed`는 여러 채팅방의 데이터를 분산하는 데 적합하지만, 하나의 인기 채팅방에 쓰기가 집중되면 해당 채팅방의 데이터는 특정 샤드에 몰릴 수 있다.

이러한 상황에 대응하려면 다음 방법을 추가로 검토할 수 있다.

- `room + bucket` 형태의 복합 샤드 키
- 시간 단위 버킷 적용
- 메시지 큐를 이용한 쓰기 버퍼링

---

## 5. Redis Cluster

### Replica와 Cluster 비교

처음에는 Redis Primary와 Replica를 다음과 같이 구성했다.

```text
Backend
   │ 읽기·쓰기
   ▼
Redis Primary
   │ 비동기 복제
   ▼
Redis Replica
```

하지만 백엔드는 계속 Primary에만 연결되어 있었다. Replica는 데이터 복제의 기반을 제공하지만 현재 구성에서는 읽기 분산이나 자동 장애 전환이 적용되지 않았고, 쓰기 요청 역시 Primary에 집중됐다.

이에 따라 쓰기 데이터를 실제로 여러 노드에 분산하기 위해 Redis Cluster로 확장했다.

### 클러스터 구성 및 결과

Redis Cluster의 16,384개 해시 슬롯을 세 개의 Master에 나눠 구성했다.

```text
Redis 1 → 0~5460
Redis 2 → 5461~10922
Redis 3 → 10923~16383
```

테스트 키 300개를 입력한 결과는 다음과 같았다.

| Redis 노드 | 키 개수 |
| --- | ---: |
| Redis 1 | 106개 |
| Redis 2 | 98개 |
| Redis 3 | 96개 |

이를 통해 일반적인 Redis key가 해시 슬롯에 따라 세 Master로 분산되는 것을 확인했다.

다만 이 결과는 서로 다른 테스트 key를 입력해 Redis Cluster 자체의 슬롯 분산을 확인한 것이다. 실제 채팅 애플리케이션의 데이터가 세 Master에 같은 비율로 분산됐다는 의미는 아니다.

### Redis key 구조의 한계

MongoDB는 문서 내부의 `room` 필드를 샤드 키로 사용할 수 있다.

```json
{
  "room": "room-123",
  "content": "메시지"
}
```

반면 Redis Cluster는 내부 field가 아니라 최상위 Redis key를 기준으로 해시 슬롯을 결정한다.

기존 구조처럼 모든 채팅방 데이터를 하나의 key에 저장하면 해당 데이터 전체가 하나의 슬롯과 하나의 Master에 배치될 수 있다.

```text
Redis key: socketio:chat-data
Map field: room-123
Map field: room-456
Map field: room-789
```

채팅방별로 데이터를 분산하려면 다음과 같이 방마다 별도의 key를 사용해야 한다.

```text
socketio:chat-data:room-123
socketio:chat-data:room-456
socketio:chat-data:room-789
```

Redis Cluster를 구축했다고 해서 애플리케이션 데이터가 자동으로 균등하게 분산되는 것은 아니다. 실제 분산 효과를 얻으려면 `RedisChatDataStore`의 key 구조와 애플리케이션의 접근 패턴까지 함께 변경해야 한다는 점을 확인했다.

---

## 6. 최종 회고

이번 프로젝트에서 MongoDB Sharding과 Redis Cluster를 직접 구성하며, 분산 시스템은 서버 수를 늘리는 것뿐 아니라 데이터 접근 패턴에 맞는 샤드 키와 Redis key 설계가 함께 필요하다는 점을 배웠다. MongoDB 메시지는 두 샤드에 약 54:46으로 분산됐고, Redis Cluster는 테스트 key를 세 Master에 106:98:96으로 분산했다.

짧은 시간 동안 집중적으로 진행된 해커톤을 통해 제한된 시간 안에 기술을 선택하고 인프라에 적용하는 경험도 할 수 있었다. 완벽한 설계를 오래 고민하기보다 현재 부하 시나리오에서 가장 중요한 문제를 정하고, 실행 가능한 구성을 빠르게 선택해 검증하는 과정이었다.

반면 샤딩 적용 전 기준값이 없어 성능 향상률을 비교하지 못했고, 반복 배포 중 S3 환경변수가 누락되어 해커톤 실전에서 파일 오프로딩 효과를 검증하지 못했다.

모니터링 에이전트가 테스트 대상 서버에 추가 부하를 줄 수 있다고 판단해 별도의 수집 도구 대신 `top`과 CloudWatch를 사용했다. 그러나 서버별 지표를 각각 확인해야 했기 때문에 동일한 시간대의 자원 사용량과 오류 발생 시점을 연결하기 어려웠다. 모니터링 오버헤드를 줄이는 것뿐 아니라 병목을 판단할 수 있는 관측 데이터를 확보하는 것도 중요하다는 점을 배웠다.

빠른 적용 과정에서 일부 검증 절차가 누락되기도 했다. 이를 통해 빠르게 결정하는 것만큼 무엇을 반드시 검증할지 함께 정하는 것이 중요하다는 점을 배웠다. 가장 크게 배운 점은 **구축했다는 사실과 효과를 검증했다는 사실은 다르다**는 것이다. 배포 역시 프로세스 실행이 아니라 핵심 기능의 정상 동작을 확인해야 완료된 것으로 볼 수 있다.

앞으로는 변경 전 기준값을 먼저 확보하고 동일한 조건에서 변경 후 결과를 비교할 것이다. 배포 단계별 스모크 테스트를 추가하고, Prometheus·Grafana 또는 CloudWatch Dashboard를 통해 여러 서버의 지표를 같은 시간축에서 수집해 장애 지점과 개선 효과를 수치로 설명할 수 있도록 개선하고 싶다.
